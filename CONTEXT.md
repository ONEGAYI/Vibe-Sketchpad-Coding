# CONTEXT.md

file-tree 技能与本仓库的领域术语表。只收术语定义，不收实现与操作细节（那些属于技能 SKILL.md 与其 references）。

## 术语

- **tree.json**：文件树数据的唯一事实源，JSON 嵌套即目录嵌套。
- **视图（view）**：登记在 tree.json 中的渲染配置实体，由唯一 id、过滤器与绑定文档清单组成。视图不是第二棵树，是同一份 tree.json 数据的切片投影；一个视图把同一份渲染内容镜像到它的全部绑定文档。
- **默认视图（default view）**：隐式视图，不带 id，固定渲染到 AGENTS.md 的树块与标签词表块。不出现在配置中、不可删除；无配置的仓库只含默认视图，行为与历史版本一致。
- **标记块（block）**：文档中由起止标记行界定、由脚本渲染占位的区域，以所属视图的 id 标识（默认视图的块不带 id）。一个文档内同一 id 至多一个块；块内内容是渲染产物，禁止手改。
- **过滤器（filter）**：视图的范围定义，一棵由"与/或/非/路径/标签"原语构成的表达式树，在 tree.json 全量数据上求值，选出该视图的条目集合。
- **剪影（silhouette）**：过滤器结果的渲染形态——选中集合投回全树结构，仅保留含有选中后代的祖先目录作为路径骨架；骨架目录只作容器，不显示简介。
